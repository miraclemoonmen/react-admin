# docs/req

本目录用于承载 `WinRide-Console` 单需求记录。

默认约定：

- 一个需求对应一个 Markdown 文件
- 文件名使用 `REQ-YYYYMMDD-XX-简短名称.md`
- 新增、迁移或重命名需求文件时，同步更新 `docs/req/INDEX.md`
- 已完成需求的后续追加、补丁和复核，默认继续回写原文件
- 需求记录必须保留目标、范围、非范围、接口影响、实际改动、验证结果和遗留问题
